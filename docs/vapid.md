# VAPID / Web Push (push contract)

**Not in Platform V1.** Do not request notification permission until this SKU is live.

- VAPID keys on the API host. Public key in the SPA only when entitled **and** deployed.
- Triggers: new announcements; truck **delayed** / **signal-lost**. Not every GPS fix.
- Public device subscriptions + signed-in custom. Service worker / PWA with the photos or tracker package as needed.
