# R2 / photo storage (photos contract)

**Not in Platform V1.**

When contracted:

- Cloudflare R2 (or S3-compatible) is source of truth for Library and gallery.
- **Signed-in** uploads only. Presigned PUT. Rate-limit. Organizer delete.
- Per-account and per-event quotas in the SOW.
- Thumbnails on cell; no full-res by default on the road.
- Local filesystem fallback for development only.
