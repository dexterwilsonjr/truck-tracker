# Database
DATABASE_URL=postgres://truck:truck@127.0.0.1:5432/truck_tracker

# API
PORT=8787
NODE_ENV=development
SESSION_SECRET=dev-session-secret-change-me
FRONTEND_ORIGIN=http://localhost:5173
COOKIE_NAME=tt_session

# Mail (optional — logs to console if unset)
MAIL_FROM=Truck Tracker <noreply@localhost>
# RESEND_API_KEY=
# SMTP_URL=

# Seed (npm run seed)
PLATFORM_ADMIN_EMAIL=platform@localhost
PLATFORM_ADMIN_PASSWORD=changeme
SEED_ORGANIZER_EMAIL=organizer@localhost
SEED_ORGANIZER_PASSWORD=changeme
SEED_BAND_SLUG=tobago-carnival
SEED_BAND_NAME=Tobago Carnival

# SPA (Vite) — copy the VITE_ line into a separate .env if you like
# Unset = sales demo. Set to enable production V1 shell + upsells.
# VITE_API_URL=http://127.0.0.1:8787
