# Truck Tracker (UI)

V1: this folder’s live map stays the **sales demo** schematic. Production routes render `UpsellScreen` for `truck_tracker` until the package is entitled and deployed.

Replace `MapPanel` internals with MapLibre in the tracker contract. Never label a schematic as live.
