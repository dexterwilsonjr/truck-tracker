# Truck Tracker — photos + Library

V1: gallery tab and `/library` render the Photos upsell (plus Face / Print / Tracker cards).

Contract build: R2, signed-in Library, organizer moderation. See `server/modules/photos/`.
