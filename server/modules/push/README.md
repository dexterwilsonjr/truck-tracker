# Push (`push`)

**Status:** contract package. **Not in Platform V1.** No VAPID send.

Web Push for announcements and truck **delays / signal-lost** — not a ping on every GPS fix. Depends on `updates` and/or `truck_tracker`.

Public device alerts + signed-in custom notifications. Ask permission only when the SKU is live. Lives in Updates / account — **not** a fifth bottom tab.
