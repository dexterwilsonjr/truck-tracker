# Print (`print`)

**Status:** contract package. **Not in Platform V1.**

Patron picks a Library photo → we send **that image file + order** to a print partner. Status: `requested` → `accepted` → `ready`.

No card checkout in-app until custom payments exist. Partner does **not** receive government ID or Tobago ID claims. Depends on `photos`. Not a bottom tab.
