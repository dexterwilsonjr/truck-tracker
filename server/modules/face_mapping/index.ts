/** Face mapping SKU — not deployed in Platform V1. */
export const MODULE = "face_mapping" as const
export const DEPLOYED = false

export const HONESTY =
  "Paint, mud and night mean a match can miss. Opt-in only. Hits land in Library — not guaranteed ID."
