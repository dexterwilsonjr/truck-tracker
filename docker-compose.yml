services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: truck
      POSTGRES_PASSWORD: truck
      POSTGRES_DB: truck_tracker
    ports:
      - "5432:5432"
    volumes:
      - truck_tracker_pg:/var/lib/postgresql/data

volumes:
  truck_tracker_pg:
